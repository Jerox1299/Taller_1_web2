// Imprimir Arreglo
function imprimirArreglo(arr) {
    for (let i = 0; i < arr.length; i++) {
        console.log(arr[i]);
    }
}

// Código de prueba
imprimirArreglo([1, "Hola", 2, "Mundo"]);
